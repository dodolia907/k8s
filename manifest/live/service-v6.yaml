apiVersion: v1
kind: Service
metadata:
  name: rtmp-service-v6
  labels:
    app: rtmp-live-server
    io.cilium/lb-pool: ula
  namespace: live
  annotations:
    external-dns.alpha.kubernetes.io/hostname: live.svc.ddlia.com
    external-dns.alpha.kubernetes.io/provider: "pihole"
spec:
  ipFamilyPolicy: SingleStack
  ipFamilies:
  - IPv6
  selector:
    app: rtmp-live-server
  type: LoadBalancer
  ports:
    - name: rtmp
      protocol: TCP
      port: 1935
      targetPort: 1935
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
