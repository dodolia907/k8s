apiVersion: v1
kind: Service
metadata:
  name: murchace-v6
  namespace: murchace
  annotations:
    external-dns.alpha.kubernetes.io/hostname: murchace.svc.ddlia.com
    io.cilium/lb-ipam-pools: "ipv6-ndp-pool-ula"
spec:
  ipFamilyPolicy: SingleStack
  ipFamilies:
  - IPv6
  selector:
    app: murchace
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000